<div class="container mx-auto pt-8 p-8">
<%= render "partials/mainnav" %>
<p id="notice"><%= notice %></p>

  <div class="flex items-center">
    <div class="w-1/5"></div>
    <div class="w-3/5">
    <h1 class="h1 mb-8 w-full text-center lowercase h1 pb-4 mb-2 font-hairline"><%= @gin.name %></h1>
    <h2 class="block w-full text-center lowercase mb-4 font-hairline"><%= @gin.distillery.name %></h2>
    <%= image_tag @gin.pic.url(:medium), class: "border shadow-lg w-full text-center" %>
    <p class="text-grey-darker font-light tracking-wide w-full p-4 border-b leading-loose" style="font-family: 'Pavanam', serif;">
      <%= @gin.text %>
    </p>
    </div>
    <div class="w-1/5"></div>
  </div>


</div>

<%= render "partials/footer" %>
