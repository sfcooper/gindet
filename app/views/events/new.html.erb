<div class="container mx-auto pt-8 p-8">
  <%= render "partials/mainnav" %>
  <h1 class="h1 mb-8 lowercase pb-4 mb-2 font-hairline">add new event</h1>

  <%= render 'form', event: @event %>

  <%= link_to 'Back', events_path %>
</div>

<%= render "partials/footer" %>
